getLzEndpointId.t.sol
├── given no LayerZero endpoint ID is registered for the provided EVM chain ID
│  └── it should revert with LzEndpointIdNotRegistered
└── given a LayerZero endpoint ID is registered for the provided EVM chain ID
   └── it should return the registered LayerZero endpoint ID
