[profile.default]
src = "src"
out = "out"
libs = ["lib"]
dynamic_test_linking = false
evm_version = "prague"
solc_version = "0.8.35"
optimizer = true
ffi = true
fs_permissions = [{ access = "read-write", path = "./"}]

# Only compile contracts which require IR-based codegen
[profile.ir]
src = "src-ir"
test = "test-ir"
script = "script-ir"
libs = ["lib/enso-weiroll"]
out = "out-ir-based"
via_ir = true

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
base = "${BASE_RPC_URL}"
arbitrum_one = "${ARBITRUM_RPC_URL}"
optimism = "${OPTIMISM_RPC_URL}"
ink = "${INK_RPC_URL}"
avalanche = "${AVALANCHE_RPC_URL}"
unichain = "${UNICHAIN_RPC_URL}"
worldchain = "${WORLDCHAIN_RPC_URL}"
monad = "${MONAD_RPC_URL}"
plasma = "${PLASMA_RPC_URL}"
robinhood = "${ROBINHOOD_RPC_URL}"
hyper_evm = "${HYPER_EVM_RPC_URL}"
polygon = "${POLYGON_RPC_URL}"

[etherscan]
1 = { key = "${MAINNET_ETHERSCAN_API_KEY}", url = "${MAINNET_ETHERSCAN_API_URL}" }
8453 = { key = "${BASE_ETHERSCAN_API_KEY}", url = "${BASE_ETHERSCAN_API_URL}" }
42161 = { key = "${ARBITRUM_ETHERSCAN_API_KEY}", url = "${ARBITRUM_ETHERSCAN_API_URL}" }
10 = { key = "${OPTIMISM_ETHERSCAN_API_KEY}", url = "${OPTIMISM_ETHERSCAN_API_URL}" }
57073 = { key = "${INK_ETHERSCAN_API_KEY}", url = "${INK_ETHERSCAN_API_URL}" }
43114 = { key = "${AVALANCHE_ETHERSCAN_API_KEY}", url = "${AVALANCHE_ETHERSCAN_API_URL}" }
130 = { key = "${UNICHAIN_ETHERSCAN_API_KEY}", url = "${UNICHAIN_ETHERSCAN_API_URL}" }
480 = { key = "${WORLDCHAIN_ETHERSCAN_API_KEY}", url = "${WORLDCHAIN_ETHERSCAN_API_URL}" }
143 = { key = "${MONAD_ETHERSCAN_API_KEY}", url = "${MONAD_ETHERSCAN_API_URL}" }
9745 = { key = "${PLASMA_ETHERSCAN_API_KEY}", url = "${PLASMA_ETHERSCAN_API_URL}" }
4663 = { key = "${ROBINHOOD_ETHERSCAN_API_KEY}", url = "${ROBINHOOD_ETHERSCAN_API_URL}" }
999 = { key = "${HYPER_EVM_ETHERSCAN_API_KEY}", url = "${HYPER_EVM_ETHERSCAN_API_URL}" }
137 = { key = "${POLYGON_ETHERSCAN_API_KEY}", url = "${POLYGON_ETHERSCAN_API_URL}" }

[fuzz]
runs = 1024
